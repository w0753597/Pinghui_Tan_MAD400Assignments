import { ElementRef } from '@angular/core';
import { InteractiveHighlightDirective } from './interactive-highlight.directive';

describe('InteractiveHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractiveHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
