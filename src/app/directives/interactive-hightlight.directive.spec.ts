import { InteractiveHightlightDirective } from './interactive-hightlight.directive';

describe('InteractiveHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractiveHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
